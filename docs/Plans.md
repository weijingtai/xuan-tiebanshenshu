# xuan-tiebanshenshu 项目开发计划

> **TODO（占位）**：这是项目专属覆写文件，覆盖上游 `weijingtai/docs/Plans.md` 的示例。
> 维护者：请按下面的骨架填写实际内容。
> 上游示例可参考 `docs/previous_archived/` 或重新拉取 weijingtai/docs 查看 Plans.md。

---

## 一、项目目标

TODO：用 1–3 句话描述 xuan-tiebanshenshu 要解决什么问题、面向哪类用户。

## 二、支持的功能 / 模块

TODO：按 `lib/features/` 下的实际子目录列出。当前已有：

- `kao_ding_liu_qin` —
- `six_yao_gua` —
- `yuan_tang_gua` —
- `kao_ke` —
- `liuqinkaoke` —
- `huang_ji` —

## 三、总体原则

TODO：复用 `docs/ai/principles.md` 的 7 条原则，或追加项目专属约束。

## 四、推荐目录结构

详见 `docs/ai/directory-structure.md`（也是覆写过的项目专属版本）。

## 五、开发阶段

TODO：列出当前阶段（如 V1 重构、迁移、稳定化）和下一阶段。

## 六、里程碑

TODO：可量化的交付节点 + 日期。

## 七、风险点

TODO：技术风险、进度风险、外部依赖风险。
